spring:
  application:
    name: auth-user-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

